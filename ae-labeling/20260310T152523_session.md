### [Continue](https://continue.dev) session transcript
 Exported: 10/03/2026 15:25:23

#### _User_

> Could you write tests for my django application ae-labeling ?

#### _Assistant_

> I'd be happy to help you write tests for your Django application! Let me first explore the project structure to understand what needs to be tested.
> 
> 

#### _Assistant_

> .git/
> .gitignore
> .pre-commit-config.yaml
> .python-version
> .virtual_documents/
> .vscode/
> README.md
> ae-labeling/
> ae-scraping/
> llm_results/
> ml_classification/
> ocs/
> pyproject.toml
> uv.lock

#### _Assistant_

> </think>
> 
> 

#### _Assistant_

> .env
> .gitignore
> .venv/
> Dockerfile
> README.md
> ae_labeling/
> app/
> data/
> docker-compose.yml
> manage.py
> pyproject.toml
> uv.lock

#### _Assistant_

> __pycache__/
> admin.py
> management/
> migrations/
> models.py
> static/
> templates/
> templatetags/
> urls.py
> views.py

#### _Assistant_

> </think>
> 
> Let me explore the main components of your Django application to understand what needs testing:
> 
> 
> 
> 

#### _Assistant_

> from django.urls import path
> from django.contrib.auth import views as auth_views
> from . import views
> 
> urlpatterns = [
>     # Main labeling view (shows next unvalidated document)
>     path("", views.labeling_view, name="labeling"),
>     # Specific document view (for admin/re-labeling)
>     path(
>         "document/<int:doc_id>/", views.labeling_document_view, name="labeling_document"
>     ),
>     # Lock management endpoints
>     path("api/release-lock/", views.release_lock_view, name="release_lock"),
>     path(
>         "api/check-lock/<int:doc_id>/",
>         views.check_lock_status,
>         name="check_lock_status",
>     ),
>     # Auth routes
>     path(
>         "login/", auth_views.LoginView.as_view(template_name="login.html"), name="login"
>     ),
>     path("logout/", auth_views.LogoutView.as_view(), name="logout"),
> ]
> 

#### _Assistant_

> from datetime import datetime
> 
> from django.shortcuts import render, redirect, get_object_or_404
> from django.contrib.auth.decorators import login_required
> from django.http import JsonResponse
> from django.conf import settings
> from django.utils import timezone
> 
> from .models import Document
> 
> 
> @login_required
> def labeling_view(request):
>     """Main view - shows next available document or handles validation"""
> 
>     # If POST, save the validation
>     if request.method == "POST":
>         doc_id = request.POST.get("doc_id")
>         validated_preds = {}
> 
>         # The 4 labels
>         labels = settings.AE_LABELS
> 
>         for label in labels:
>             # Check if the checkbox was sent in form (value="on")
>             validated_preds[label] = request.POST.get(label) == "on"
> 
>         doc = get_object_or_404(Document, id=doc_id)
> 
>         # Verify this user has the lock before allowing save
>         if doc.is_locked() and doc.locked_by != request.user:
>             return render(
>                 request,
>                 "labeling.html",
>                 {
>                     "doc": None,
>                     "error": f"Document is being validated by {doc.locked_by.username}",
>                 },
>             )
> 
>         doc.validated_predictions = validated_preds
>         doc.is_validated = True
>         doc.validated_by = request.user
>         doc.validated_at = timezone.now()
>         doc.save()
> 
>         # Release the lock (unlock will set locked_by to None)
>         doc.unlock()
> 
>         # Redirect to next unvalidated document
>         return redirect("labeling")
> 
>     # If GET, first release any existing lock held by this user
>     existing_locked_docs = Document.objects.filter(locked_by=request.user).all()
>     for doc in existing_locked_docs:
>         doc.unlock()  # Release previous lock before getting new one
> 
>     next_doc = Document.get_next_unlocked_document()
> 
>     if not next_doc:
>         return render(request, "labeling.html", {"doc": None})
> 
>     # Try to acquire lock on this document
>     success, _ = next_doc.lock(request.user)
> 
>     if not success:
>         # If we couldn't lock it, try to find another one
>         # This handles the race condition where two users clicked at same time
>         return redirect("labeling")  # Refresh and try again
> 
>     return render(request, "labeling.html", {"doc": next_doc})
> 
> 
> @login_required
> def labeling_document_view(request, doc_id):
>     """View for accessing a specific document by ID (for admin/re-labeling)"""
> 
>     # If POST, save the validation
>     if request.method == "POST":
>         validated_preds = {}
> 
>         # The 4 labels
>         labels = settings.AE_LABELS
> 
>         for label in labels:
>             # Check if the checkbox was sent in form (value="on")
>             validated_preds[label] = request.POST.get(label) == "on"
> 
>         doc = get_object_or_404(Document, id=doc_id)
> 
>         doc.validated_predictions = validated_preds
>         doc.is_validated = True
>         doc.validated_by = request.user
>         doc.validated_at = timezone.now()
> 
>         doc.save()
> 
>         # Redirect to next unvalidated document or back to admin
>         next_unvalidated = Document.get_next_unlocked_document()
>         if next_unvalidated:
>             return redirect("labeling_document", doc_id=next_unvalidated.id)
>         else:
>             return redirect("admin:app_document_changelist")
> 
>     # If GET, get the specific document by ID, not checking the lock as we are in case of admin access
>     doc = get_object_or_404(Document, id=doc_id)
> 
>     return render(request, "labeling.html", {"doc": doc})
> 
> 
> @login_required
> def release_lock_view(request):
>     """API endpoint to manually release a lock (e.g., user closes browser)"""
> 
>     if request.method == "POST":
>         doc_id = request.POST.get("doc_id")
>         doc = get_object_or_404(Document, id=doc_id)
> 
>         # Only allow the user who has the lock to release it
>         if doc.locked_by and doc.locked_by == request.user:
>             doc.unlock()
>             return JsonResponse({"success": True})
> 
>     return JsonResponse({"error": "Cannot release this lock"}, status=403)
> 
> 
> @login_required
> def check_lock_status(request, doc_id):
>     """API endpoint to check if a document is locked"""
> 
>     doc = get_object_or_404(Document, id=doc_id)
> 
>     return JsonResponse(
>         {
>             "is_locked": doc.is_locked(),
>             "locked_by": doc.locked_by.username if doc.locked_by else None,
>             "locked_at": doc.locked_at.isoformat() if doc.locked_at else None,
>         }
>     )
> 

#### _Assistant_

> from datetime import timedelta
> 
> from django.db import models
> from django.contrib.auth.models import User
> from django.utils import timezone
> from typing import Self
> 
> 
> class Document(models.Model):
>     # Original Data from Parquet
>     pdf_name = models.CharField(max_length=255, unique=True)
>     pdf_text = models.TextField()
>     pdf_text_raw = models.TextField()
>     contexts = models.JSONField(default=list, blank=True)  # List of strings
>     explanation = models.TextField(blank=True)
>     prediction_time = models.FloatField(null=True, blank=True)
> 
>     # Original LLM Predictions (Stored as JSON for flexibility)
>     # Structure: {"surfaces_agricoles": {"score": 0.8, "pred": True}, ...}
>     original_predictions = models.JSONField(default=dict)
> 
>     # Validation Status
>     is_validated = models.BooleanField(default=False)
>     validated_by = models.ForeignKey(
>         User, on_delete=models.SET_NULL, null=True, blank=True
>     )
>     validated_at = models.DateTimeField(null=True)
> 
>     # Corrected Predictions (Stored as JSON)
>     # Structure: {"surfaces_agricoles": True, ...}
>     validated_predictions = models.JSONField(default=dict, blank=True)
> 
>     locked_by = models.ForeignKey(
>         User,
>         on_delete=models.SET_NULL,
>         null=True,
>         blank=True,
>         related_name="locked_documents",  # Allows querying documents locked by a user
>     )
>     locked_at = models.DateTimeField(null=True, blank=True)
> 
>     def __str__(self) -> str:
>         return self.pdf_name
> 
>     def is_locked(self) -> bool:
>         """Check if document is currently locked. Can release old locks."""
> 
>         # Check if the document has been locked more than one hour ago
>         if (self.locked_by is not None) and (self.locked_at is not None):
>             if timezone.now() - self.locked_at >= timedelta(hours=1):
>                 self.unlock()
>                 return False
>             return True
> 
>         return False
> 
>     def lock(self, user) -> tuple[bool, str]:
>         """Acquire a lock on this document"""
>         # Only allow locking unvalidated documents
>         if self.is_validated:
>             return False, "Document already validated"
> 
>         # Check if already locked by someone else
>         if self.is_locked() and (self.locked_by != user):
>             return False, f"Document is being validated by {self.locked_by.username}"
> 
>         # Lock the document (or refresh lock if same user)
>         self.locked_by = user
>         self.locked_at = timezone.now()
>         self.save(update_fields=["locked_by", "locked_at"])
>         return True, "Document locked successfully"
> 
>     def unlock(self) -> bool:
>         """Release the lock on this document"""
>         if self.locked_by:
>             self.locked_by = None
>             self.locked_at = None
>             self.save(update_fields=["locked_by", "locked_at"])
>             return True
>         return False
> 
>     @classmethod
>     def get_next_unlocked_document(cls) -> Self | None:
>         """Get next document that is not validated and not locked. Release tale locks"""
>         LOCK_TIMEOUT = timedelta(minutes=60)  # Lock expires after 10 minutes
> 
>         now = timezone.now()
> 
>         expired_docs = cls.objects.filter(
>             is_validated=False, locked_at__lt=now - LOCK_TIMEOUT
>         )
> 
>         for expired_doc in expired_docs:
>             expired_doc.unlock()  # Release expired lock
> 
>         # First try to find truly unlocked documents
>         doc = cls.objects.filter(is_validated=False, locked_by=None).first()
> 
>         return doc
> 

#### _Assistant_

> </think>
> 
> Now let me check if there are any existing tests and look at the settings:
> 
> 
> 

#### _Assistant_

> [project]
> name = "ae-labeling"
> version = "0.1.0"
> description = "Web application to validate LLM predictions for AE PDFs."
> readme = "README.md"
> requires-python = ">=3.13"
> dependencies = [
>     "django>=6.0.2",
>     "django-environ>=0.13.0",
>     "gunicorn>=25.1.0",
>     "polars>=1.38.0",
>     "psycopg2-binary>=2.9.11",
> ]
> 
> [dependency-groups]
> dev = ["pytest>=7.0", "black>=23.0", "django-debug-toolbar>=6.2.0"]
> 

#### _Assistant_

> No matches found. Build, secrets, etc. dirs and files are not included.

#### _Assistant_

> </think>
> 
> Let me check the Django settings file:
> 
> 

#### _Assistant_

> ae-labeling/ae_labeling/settings.py
> 

#### _Assistant_

> </think>
> 
> 

#### _Assistant_

> """
> Django settings for ae_labeling project.
> 
> Generated by 'django-admin startproject' using Django 6.0.2.
> 
> For more information on this file, see
> https://docs.djangoproject.com/en/6.0/topics/settings/
> 
> For the full list of settings and their values, see
> https://docs.djangoproject.com/en/6.0/ref/settings/
> """
> 
> from pathlib import Path
> 
> import environ
> 
> 
> # Build paths inside the project like this: BASE_DIR / 'subdir'.
> BASE_DIR = Path(__file__).resolve().parent.parent
> 
> env = environ.Env(DEBUG=(bool, False))
> environ.Env.read_env(BASE_DIR / ".env")
> 
> # Quick-start development settings - unsuitable for production
> # See https://docs.djangoproject.com/en/6.0/howto/deployment/checklist/
> 
> # SECURITY WARNING: keep the secret key used in production secret!
> SECRET_KEY = env("DJANGO_SECRET_KEY")
> 
> # SECURITY WARNING: don't run with debug turned on in production!
> DEBUG = env("DJANGO_DEBUG")
> 
> ALLOWED_HOSTS = ["localhost"] + env.list("ALLOWED_HOSTS")
> 
> 
> # Application definition
> 
> INSTALLED_APPS = [
>     "django.contrib.admin",
>     "django.contrib.auth",
>     "django.contrib.contenttypes",
>     "django.contrib.sessions",
>     "django.contrib.messages",
>     "django.contrib.staticfiles",
>     "app",
> ]
> 
> 
> MIDDLEWARE = [
>     "django.middleware.security.SecurityMiddleware",
>     "django.contrib.sessions.middleware.SessionMiddleware",
>     "django.middleware.common.CommonMiddleware",
>     "django.middleware.csrf.CsrfViewMiddleware",
>     "django.contrib.auth.middleware.AuthenticationMiddleware",
>     "django.contrib.messages.middleware.MessageMiddleware",
>     "django.middleware.clickjacking.XFrameOptionsMiddleware",
> ]
> 
> ROOT_URLCONF = "ae_labeling.urls"
> 
> TEMPLATES = [
>     {
>         "BACKEND": "django.template.backends.django.DjangoTemplates",
>         "DIRS": [],
>         "APP_DIRS": True,
>         "OPTIONS": {
>             "context_processors": [
>                 "django.template.context_processors.debug",
>                 "django.template.context_processors.request",
>                 "django.contrib.auth.context_processors.auth",
>                 "django.contrib.messages.context_processors.messages",
>             ],
>         },
>     },
> ]
> 
> WSGI_APPLICATION = "ae_labeling.wsgi.application"
> 
> 
> # Database
> # https://docs.djangoproject.com/en/6.0/ref/settings/#databases
> 
> DATABASES = {
>     "default": {
>         "ENGINE": "django.db.backends.postgresql",
>         "NAME": env("PG_DBNAME"),
>         "USER": env("PG_USER"),
>         "PASSWORD": env("PG_PASSWORD"),
>         "HOST": env("PG_HOST"),
>         "PORT": env("PG_PORT", default=5432),
>     }
> }
> 
> 
> # Password validation
> # https://docs.djangoproject.com/en/6.0/ref/settings/#auth-password-validators
> 
> AUTH_PASSWORD_VALIDATORS = [
>     {
>         "NAME": "django.contrib.auth.password_validation.UserAttributeSimilarityValidator",
>     },
>     {
>         "NAME": "django.contrib.auth.password_validation.MinimumLengthValidator",
>     },
>     {
>         "NAME": "django.contrib.auth.password_validation.CommonPasswordValidator",
>     },
>     {
>         "NAME": "django.contrib.auth.password_validation.NumericPasswordValidator",
>     },
> ]
> 
> 
> # Static files (CSS, JavaScript, Images)
> # https://docs.djangoproject.com/en/6.0/howto/static-files/
> 
> STATIC_URL = "static/"
> STATICFILES_DIRS = [BASE_DIR / "app/static"]
> 
> DEFAULT_AUTO_FIELD = "django.db.models.BigAutoField"
> 
> LOGIN_URL = "/login/"
> LOGIN_REDIRECT_URL = "/"
> 
> TIME_ZONE = "Europe/Paris"
> 
> AE_LABELS = [
>     "surfaces_agricoles",
>     "surfaces_artificialisees",
>     "surfaces_forestieres",
>     "surfaces_naturelles",
> ]
> 
> LABELS_COLOR_MAP = {
>     "surfaces_agricoles": "#f6b93b",
>     "surfaces_artificialisees": "#e55039",
>     "surfaces_forestieres": "#78e08f",
>     "surfaces_naturelles": "#6a89cc",
> }
> 

#### _Assistant_

> </think>
> 
> Perfect! Now I have a good understanding of your Django application. Let me also check if there's an admin configuration:
> 
> 

#### _Assistant_

> from django.contrib import admin
> from django.urls import reverse
> from django.utils.html import format_html, mark_safe
> 
> from .models import Document
> 
> 
> @admin.register(Document)
> class DocumentAdmin(admin.ModelAdmin):
>     list_display = [
>         "pdf_name",
>         "is_validated",
>         "validated_by",
>         "validated_at",
>         "get_prediction_summary",
>         "labeling_link",  # Add this new column
>     ]
> 
>     list_filter = ["is_validated", "validated_at"]
> 
>     search_fields = ["pdf_name", "contexts"]
> 
>     readonly_fields = [
>         "pdf_name",
>         "pdf_text_raw",
>         "explanation",
>         "original_predictions_display",
>         "validated_predictions_display",
>         "prediction_time",
>         "validated_by",
>         "validated_at",
>     ]
> 
>     fieldsets = (
>         ("Document Info", {"fields": ("pdf_name", "is_validated")}),
>         (
>             "Validation Details",
>             {"fields": ("validated_by", "validated_at", "prediction_time")},
>         ),
>         (
>             "Predictions",
>             {
>                 "fields": (
>                     "original_predictions_display",
>                     "validated_predictions_display",
>                 )
>             },
>         ),
>         (
>             "Content",
>             {"fields": ("pdf_text_raw", "explanation"), "classes": ["collapse"]},
>         ),
>     )
> 
>     def get_prediction_summary(self, obj):
>         """Show a summary of validated predictions"""
>         if not obj.is_validated:
>             return "Not validated"
> 
>         true_labels = [k for k, v in obj.validated_predictions.items() if v]
>         if true_labels:
>             return f"{len(true_labels)} labels: {', '.join(true_labels)}"
>         return "No labels selected"
> 
>     get_prediction_summary.short_description = "Predictions"
> 
>     def labeling_link(self, obj):
>         """Create a link to the document's labeling page"""
>         url = reverse("labeling_document", kwargs={"doc_id": obj.id})
>         if obj.is_validated:
>             return format_html(
>                 '<a href="{}" class="btn btn-sm btn-outline-primary">View/Edit</a>', url
>             )
>         else:
>             return format_html(
>                 '<a href="{}" class="btn btn-sm btn-success">Label Now</a>', url
>             )
> 
>     labeling_link.short_description = "Actions"
> 
>     def original_predictions_display(self, obj):
>         """Display original predictions in a readable format"""
>         if not obj.original_predictions:
>             return mark_safe("<em>No predictions</em>")
> 
>         html = '<table style="width: 100%; border-collapse: collapse; margin-top: 5px;"><tbody>'
>         for label, data in obj.original_predictions.items():
>             pred = "✓" if data.get("pred") else "✗"
>             score = f"{data.get('score', 0):.2f}"
>             safe_label = format_html("{}", label)
>             html += (
>                 f'<tr><td style="padding: 4px 8px; border-bottom: 1px solid #ddd;">{safe_label}</td>'
>                 f'<td style="padding: 4px 8px; border-bottom: 1px solid #ddd;">{pred}</td>'
>                 f'<td style="padding: 4px 8px; border-bottom: 1px solid #ddd;">{score}</td></tr>'
>             )
>         html += "</tbody></table>"
> 
>         return mark_safe(html)
> 
>     original_predictions_display.short_description = "Original Predictions"
> 
>     def validated_predictions_display(self, obj):
>         """Display validated predictions in a readable format"""
>         if not obj.validated_predictions:
>             return mark_safe("<em>Not validated</em>")
> 
>         html = '<table style="width: 100%; border-collapse: collapse; margin-top: 5px;"><tbody>'
>         for label, is_selected in obj.validated_predictions.items():
>             status = "✓ Selected" if is_selected else "✗ Not selected"
>             safe_label = format_html("{}", label)
>             html += (
>                 f'<tr><td style="padding: 4px 8px; border-bottom: 1px solid #ddd;">{safe_label}</td>'
>                 f'<td style="padding: 4px 8px; border-bottom: 1px solid #ddd;">{status}</td></tr>'
>             )
>         html += "</tbody></table>"
> 
>         return mark_safe(html)
> 
>     validated_predictions_display.short_description = "Validated Predictions"
> 
>     def has_add_permission(self, request):
>         """Prevent adding documents through admin"""
>         return False
> 
>     def has_delete_permission(self, request, obj=None):
>         """Allow deleting only if not validated (optional)"""
>         if obj and obj.is_validated:
>             return False
>         return True
> 

#### _Assistant_

> </think>
> 
> Excellent! Now I have a comprehensive understanding of your Django application. Let me create a detailed testing plan for you.
> 
> ## Testing Plan Summary
> 
> Based on my analysis, here's what I found in your `ae-labeling` application:
> 
> ### Application Structure:
> - **Model**: `Document` - stores PDF documents with LLM predictions and validation data
> - **Views**: 4 views (labeling, labeling_document, release_lock, check_lock_status)
> - **Features**: Document locking mechanism, user authentication, validation workflow
> - **Settings**: Uses PostgreSQL, environment variables, custom AE_LABELS configuration
> 
> ### Recommended Test Coverage:
> 
> I'll create comprehensive tests covering:
> 
> 1. **Model Tests** (`test_models.py`):
>    - Document creation and validation
>    - Lock/unlock functionality
>    - Lock expiration logic
>    - `get_next_unlocked_document()` method
> 
> 2. **View Tests** (`test_views.py`):
>    - Authentication requirements
>    - Labeling workflow (GET/POST)
>    - Lock management endpoints
>    - Document validation flow
> 
> 3. **Integration Tests** (`test_integration.py`):
>    - End-to-end labeling workflow
>    - Concurrent user scenarios
>    - Lock race conditions
> 
> Let me create these test files for you:
> 
> ---
> 
> ## Suggested Test Files
> 
> ### 1. Model Tests
> 
> ```python
> ae-labeling/app/tests/test_models.py
> ```
> 
> ```python
> """
> Tests for Document model and its methods.
> """
> import pytest
> from datetime import timedelta
> from django.utils import timezone
> from django.contrib.auth.models import User
> from django.test import TestCase, tag
> from app.models import Document
> 
> 
> @pytest.mark.django_db
> class DocumentModelTest(TestCase):
>     """Test cases for Document model."""
> 
>     def setUp(self):
>         """Set up test fixtures."""
>         self.user1 = User.objects.create_user(
>             username="user1", password="password123"
>         )
>         self.user2 = User.objects.create_user(
>             username="user2", password="password123"
>         )
> 
>         # Create a sample document
>         self.document = Document.objects.create(
>             pdf_name="test_document.pdf",
>             pdf_text="Test content",
>             pdf_text_raw="Raw test content",
>             original_predictions={
>                 "surfaces_agricoles": {"score": 0.8, "pred": True},
>                 "surfaces_artificialisees": {"score": 0.3, "pred": False},
>             },
>         )
> 
>     def test_document_creation(self):
>         """Test that a document can be created successfully."""
>         assert self.document.pdf_name == "test_document.pdf"
>         assert self.document.is_validated is False
>         assert self.document.locked_by is None
> 
>     def test_str_method(self):
>         """Test the __str__ method returns pdf_name."""
>         assert str(self.document) == "test_document.pdf"
> 
>     @tag("locking")
>     def test_is_locked_when_not_locked(self):
>         """Test that document reports not locked when it isn't."""
>         assert self.document.is_locked() is False
> 
>     @tag("locking")
>     def test_is_locked_when_locked(self):
>         """Test that document reports locked when it is."""
>         self.document.locked_by = self.user1
>         self.document.locked_at = timezone.now()
>         self.document.save()
>         assert self.document.is_locked() is True
> 
>     @tag("locking")
>     def test_is_locked_releases_old_lock(self):
>         """Test that is_locked releases locks older than 1 hour."""
>         old_time = timezone.now() - timedelta(hours=2)
>         self.document.locked_by = self.user1
>         self.document.locked_at = old_time
>         self.document.save()
> 
>         # is_locked should release the lock and return False
>         assert self.document.is_locked() is False
>         self.document.refresh_from_db()
>         assert self.document.locked_by is None
> 
>     @tag("locking")
>     def test_lock_success(self):
>         """Test successful document locking."""
>         success, message = self.document.lock(self.user1)
>         assert success is True
>         assert "successfully" in message.lower()
>         self.document.refresh_from_db()
>         assert self.document.locked_by == self.user1
> 
>     @tag("locking")
>     def test_lock_already_validated(self):
>         """Test that validated documents cannot be locked."""
>         self.document.is_validated = True
>         self.document.save()
> 
>         success, message = self.document.lock(self.user1)
>         assert success is False
>         assert "validated" in message.lower()
> 
>     @tag("locking")
>     def test_lock_by_different_user(self):
>         """Test that a document locked by one user cannot be locked by another."""
>         self.document.locked_by = self.user1
>         self.document.locked_at = timezone.now()
>         self.document.save()
> 
>         success, message = self.document.lock(self.user2)
>         assert success is False
>         assert self.user1.username in message
> 
>     @tag("locking")
>     def test_lock_refresh_by_same_user(self):
>         """Test that same user can refresh their lock."""
>         self.document.locked_by = self.user1
>         old_locked_at = timezone.now() - timedelta(minutes=30)
>         self.document.locked_at = old_locked_at
>         self.document.save()
> 
>         success, _ = self.document.lock(self.user1)
>         assert success is True
>         self.document.refresh_from_db()
>         assert self.document.locked_by == self.user1
>         assert self.document.locked_at > old_locked_at
> 
>     @tag("locking")
>     def test_unlock_success(self):
>         """Test successful document unlocking."""
>         self.document.locked_by = self.user1
>         self.document.locked_at = timezone.now()
>         self.document.save()
> 
>         success = self.document.unlock()
>         assert success is True
>         self.document.refresh_from_db()
>         assert self.document.locked_by is None
>         assert self.document.locked_at is None
> 
>     @tag("locking")
>     def test_unlock_when_not_locked(self):
>         """Test unlocking a document that isn't locked."""
>         success = self.document.unlock()
>         assert success is False
> 
>     @tag("querying")
>     def test_get_next_unlocked_document_returns_unlocked(self):
>         """Test that get_next_unlocked_document returns an unlocked document."""
>         result = Document.get_next_unlocked_document()
>         assert result == self.document
> 
>     @tag("querying")
>     def test_get_next_unlocked_document_skips_validated(self):
>         """Test that validated documents are skipped."""
>         self.document.is_validated = True
>         self.document.save()
> 
>         # Create an unlocked document
>         doc2 = Document.objects.create(
>             pdf_name="test_document2.pdf",
>             pdf_text="Test content 2",
>         )
> 
>         result = Document.get_next_unlocked_document()
>         assert result == doc2
> 
>     @tag("querying")
>     def test_get_next_unlocked_document_releases_expired_locks(self):
>         """Test that expired locks are released."""
>         old_time = timezone.now() - timedelta(minutes=90)  # Expired lock
>         self.document.locked_by = self.user1
>         self.document.locked_at = old_time
>         self.document.save()
> 
>         result = Document.get_next_unlocked_document()
>         assert result == self.document
>         self.document.refresh_from_db()
>         assert self.document.locked_by is None
> 
>     @tag("querying")
>     def test_get_next_unlocked_document_returns_none_when_all_locked(self):
>         """Test that None is returned when all documents are locked or validated."""
>         # Lock the document
>         self.document.locked_by = self.user1
>         self.document.locked_at = timezone.now()
>         self.document.save()
> 
>         result = Document.get_next_unlocked_document()
>         assert result is None
> 
> 
> @pytest.mark.django_db
> class DocumentValidationTest(TestCase):
>     """Test cases for document validation workflow."""
> 
>     def setUp(self):
>         """Set up test fixtures."""
>         self.user = User.objects.create_user(
>             username="validator", password="password123"
>         )
>         self.document = Document.objects.create(
>             pdf_name="validation_test.pdf",
>             pdf_text="Test content for validation",
>         )
> 
>     def test_validate_document(self):
>         """Test the complete validation workflow."""
>         # Lock the document
>         success, _ = self.document.lock(self.user)
>         assert success is True
> 
>         # Simulate validation (this would normally happen in views)
>         validated_predictions = {
>             "surfaces_agricoles": True,
>             "surfaces_artificialisees": False,
>             "surfaces_forestieres": True,
>             "surfaces_naturelles": False,
>         }
> 
>         self.document.validated_predictions = validated_predictions
>         self.document.is_validated = True
>         self.document.validated_by = self.user
>         self.document.validated_at = timezone.now()
>         self.document.save()
> 
>         # Unlock the document
>         self.document.unlock()
> 
>         # Verify validation
>         self.document.refresh_from_db()
>         assert self.document.is_validated is True
>         assert self.document.validated_by == self.user
>         assert self.document.validated_predictions == validated_predictions
>         assert self.document.locked_by is None
> 
>     def test_cannot_lock_validated_document(self):
>         """Test that validated documents cannot be locked."""
>         self.document.is_validated = True
>         self.document.save()
> 
>         success, message = self.document.lock(self.user)
>         assert success is False
> ```
> 
> ---
> 
> ### 2. View Tests
> 
> ```python
> ae-labeling/app/tests/test_views.py
> ```
> 
> ```python
> """
> Tests for app views.
> """
> import pytest
> from django.urls import reverse
> from django.contrib.auth.models import User
> from django.test import TestCase, Client
> from django.utils import timezone
> from datetime import timedelta
> from app.models import Document
> 
> 
> @pytest.mark.django_db
> class LabelingViewTest(TestCase):
>     """Test cases for labeling_view."""
> 
>     def setUp(self):
>         """Set up test fixtures."""
>         self.user = User.objects.create_user(
>             username="testuser", password="password123"
>         )
>         self.client = Client()
>         
>         # Create a sample document
>         self.document = Document.objects.create(
>             pdf_name="test_doc.pdf",
>             pdf_text="Test content",
>             original_predictions={
>                 "surfaces_agricoles": {"score": 0.8, "pred": True},
>                 "surfaces_artificialisees": {"score": 0.3, "pred": False},
>                 "surfaces_forestieres": {"score": 0.6, "pred": True},
>                 "surfaces_naturelles": {"score": 0.2, "pred": False},
>             },
>         )
> 
>     def test_labeling_view_requires_login(self):
>         """Test that labeling view requires authentication."""
>         response = self.client.get(reverse("labeling"))
>         assert response.status_code == 302
>         assert "/login/" in response.url
> 
>     def test_labeling_view_get_authenticated_user(self):
>         """Test GET request for authenticated user with available document."""
>         self.client.login(username="testuser", password="password123")
>         response = self.client.get(reverse("labeling"))
>         
>         assert response.status_code == 200
>         assert "doc" in response.context
>         assert response.context["doc"] == self.document
> 
>     def test_labeling_view_get_no_documents(self):
>         """Test GET request when no documents are available."""
>         # Mark document as validated
>         self.document.is_validated = True
>         self.document.save()
> 
>         self.client.login(username="testuser", password="password123")
>         response = self.client.get(reverse("labeling"))
> 
>         assert response.status_code == 200
>         assert response.context["doc"] is None
> 
>     def test_labeling_view_post_validates_document(self):
>         """Test POST request validates document correctly."""
>         self.client.login(username="testuser", password="password123")
> 
>         # Prepare validation data
>         post_data = {
>             "doc_id": self.document.id,
>             "surfaces_agricoles": "on",
>             "surfaces_artificialisees": "",  # Not selected
>             "surfaces_forestieres": "on",
>             "surfaces_naturelles": "",  # Not selected
>         }
> 
>         response = self.client.post(reverse("labeling"), post_data)
> 
>         # Should redirect to labeling view after validation
>         assert response.status_code == 302
>         
>         # Verify document was validated
>         self.document.refresh_from_db()
>         assert self.document.is_validated is True
>         assert self.document.validated_by == self.user
>         assert self.document.validated_predictions == {
>             "surfaces_agricoles": True,
>             "surfaces_artificialisees": False,
>             "surfaces_forestieres": True,
>             "surfaces_naturelles": False,
>         }
> 
>     def test_labeling_view_post_cannot_steal_lock(self):
>         """Test that user cannot validate document locked by another user."""
>         other_user = User.objects.create_user(
>             username="otheruser", password="password123"
>         )
>         
>         # Lock document with other user
>         self.document.locked_by = other_user
>         self.document.locked_at = timezone.now()
>         self.document.save()
> 
>         self.client.login(username="testuser", password="password123")
> 
>         post_data = {
>             "doc_id": self.document.id,
>             "surfaces_agricoles": "on",
>             "surfaces_artificialisees": "",
>             "surfaces_forestieres": "",
>             "surfaces_naturelles": "",
>         }
> 
>         response = self.client.post(reverse("labeling"), post_data)
> 
>         # Should show error, not redirect
>         assert response.status_code == 200
>         assert "error" in response.context
>         assert other_user.username in response.context["error"]
> 
> 
> @pytest.mark.django_db
> class LabelingDocumentViewTest(TestCase):
>     """Test cases for labeling_document_view."""
> 
>     def setUp(self):
>         """Set up test fixtures."""
>         self.user = User.objects.create_user(
>             username="testuser", password="password123"
>         )
>         self.client = Client()
>         
>         self.document = Document.objects.create(
>             pdf_name="specific_doc.pdf",
>             pdf_text="Specific document content",
>         )
> 
>     def test_labeling_document_view_get(self):
>         """Test GET request for specific document."""
>         self.client.login(username="testuser", password="password123")
>         response = self.client.get(
>             reverse("labeling_document", kwargs={"doc_id": self.document.id})
>         )
> 
>         assert response.status_code == 200
>         assert response.context["doc"] == self.document
> 
>     def test_labeling_document_view_404(self):
>         """Test GET request for non-existent document returns 404."""
>         self.client.login(username="testuser", password="password123")
>         response = self.client.get(
>             reverse("labeling_document", kwargs={"doc_id": 9999})
>         )
> 
>         assert response.status_code == 404
> 
>     def test_labeling_document_view_post(self):
>         """Test POST request validates specific document."""
>         self.client.login(username="testuser", password="password123")
> 
>         post_data = {
>             "surfaces_agricoles": "on",
>             "surfaces_artificialisees": "",
>             "surfaces_forestieres": "",
>             "surfaces_naturelles": "",
>         }
> 
>         response = self.client.post(
>             reverse("labeling_document", kwargs={"doc_id": self.document.id}), post_data
>         )
> 
>         assert response.status_code == 302
>         
>         self.document.refresh_from_db()
>         assert self.document.is_validated is True
> 
> 
> @pytest.mark.django_db
> class ReleaseLockViewTest(TestCase):
>     """Test cases for release_lock_view."""
> 
>     def setUp(self):
>         """Set up test fixtures."""
>         self.user = User.objects.create_user(
>             username="testuser", password="password123"
>         )
>         self.client = Client()
>         
>         self.document = Document.objects.create(
>             pdf_name="locked_doc.pdf",
>             pdf_text="Locked document content",
>             locked_by=self.user,
>             locked_at=timezone.now(),
>         )
> 
>     def test_release_lock_success(self):
>         """Test successful lock release."""
>         self.client.login(username="testuser", password="password123")
> 
>         response = self.client.post(
>             reverse("release_lock"), {"doc_id": self.document.id}
>         )
> 
>         assert response.status_code == 200
>         data = response.json()
>         assert data["success"] is True
>         
>         self.document.refresh_from_db()
>         assert self.document.locked_by is None
> 
>     def test_release_lock_wrong_user(self):
>         """Test that user cannot release another user's lock."""
>         other_user = User.objects.create_user(
>             username="otheruser", password="password123"
>         )
>         
>         # Change lock to other user
>         self.document.locked_by = other_user
>         self.document.save()
> 
>         self.client.login(username="testuser", password="password123")
> 
>         response = self.client.post(
>             reverse("release_lock"), {"doc_id": self.document.id}
>         )
> 
>         assert response.status_code == 403
>         
>         self.document.refresh_from_db()
>         assert self.document.locked_by == other_user
> 
> 
> @pytest.mark.django_db
> class CheckLockStatusViewTest(TestCase):
>     """Test cases for check_lock_status view."""
> 
>     def setUp(self):
>         """Set up test fixtures."""
>         self.user = User.objects.create_user(
>             username="testuser", password="password123"
>         )
>         self.client = Client()
>         
>         self.document = Document.objects.create(
>             pdf_name="status_doc.pdf",
>             pdf_text="Status document content",
>         )
> 
>     def test_check_lock_status_unlocked(self):
>         """Test lock status for unlocked document."""
>         self.client.login(username="testuser", password="password123")
> 
>         response = self.client.get(
>             reverse("check_lock_status", kwargs={"doc_id": self.document.id})
>         )
> 
>         assert response.status_code == 200
>         data = response.json()
>         assert data["is_locked"] is False
>         assert data["locked_by"] is

#### _User_

> Continue your response exactly where you left off:

#### _Assistant_

> None
>         assert data["locked_at"] is None
> 
>     def test_check_lock_status_locked(self):
>         """Test lock status for locked document."""
>         self.document.locked_by = self.user
>         self.document.locked_at = timezone.now()
>         self.document.save()
> 
>         self.client.login(username="testuser", password="password123")
> 
>         response = self.client.get(
>             reverse("check_lock_status", kwargs={"doc_id": self.document.id})
>         )
> 
>         assert response.status_code == 200
>         data = response.json()
>         assert data["is_locked"] is True
>         assert data["locked_by"] == self.user.username
>         assert data["locked_at"] is not None
> 
> 
> @pytest.mark.django_db
> class AuthenticationViewTest(TestCase):
>     """Test cases for authentication views."""
> 
>     def setUp(self):
>         """Set up test fixtures."""
>         self.client = Client()
>         self.user = User.objects.create_user(
>             username="testuser", password="password123"
>         )
> 
>     def test_login_view_get(self):
>         """Test login page loads correctly."""
>         response = self.client.get(reverse("login"))
>         assert response.status_code == 200
> 
>     def test_login_success(self):
>         """Test successful login."""
>         response = self.client.post(
>             reverse("login"),
>             {"username": "testuser", "password": "password123"},
>         )
>         
>         # Should redirect after successful login
>         assert response.status_code == 302
> 
>     def test_login_failure(self):
>         """Test failed login with wrong password."""
>         response = self.client.post(
>             reverse("login"),
>             {"username": "testuser", "password": "wrongpassword"},
>         )
>         
>         # Should stay on login page with error
>         assert response.status_code == 200
> 
>     def test_logout(self):
>         """Test logout functionality."""
>         self.client.login(username="testuser", password="password123")
>         
>         # Verify logged in
>         response = self.client.get(reverse("labeling"))
>         assert response.status_code == 200
>         
>         # Logout
>         response = self.client.post(reverse("logout"))
>         assert response.status_code == 302
>         
>         # Should require login again
>         response = self.client.get(reverse("labeling"))
>         assert response.status_code == 302
> ```
> 
> ---
> 
> ### 3. Integration Tests
> 
> ```python
> ae-labeling/app/tests/test_integration.py
> ```
> 
> ```python
> """
> Integration tests for the complete labeling workflow.
> """
> import pytest
> from django.urls import reverse
> from django.contrib.auth.models import User
> from django.test import TestCase, Client
> from app.models import Document
> 
> 
> @pytest.mark.django_db
> class LabelingWorkflowTest(TestCase):
>     """Integration tests for the complete document labeling workflow."""
> 
>     def setUp(self):
>         """Set up test fixtures with multiple documents and users."""
>         # Create users
>         self.user1 = User.objects.create_user(
>             username="validator1", password="password123"
>         )
>         self.user2 = User.objects.create_user(
>             username="validator2", password="password123"
>         )
> 
>         # Create multiple documents
>         for i in range(5):
>             Document.objects.create(
>                 pdf_name=f"document_{i}.pdf",
>                 pdf_text=f"Content of document {i}",
>                 original_predictions={
>                     "surfaces_agricoles": {"score": 0.8, "pred": True},
>                     "surfaces_artificialisees": {"score": 0.3, "pred": False},
>                     "surfaces_forestieres": {"score": 0.6, "pred": True},
>                     "surfaces_naturelles": {"score": 0.2, "pred": False},
>                 },
>             )
> 
>         self.client = Client()
> 
>     def test_complete_labeling_workflow(self):
>         """Test the complete workflow: login -> label document -> next document."""
>         # Login
>         self.client.login(username="validator1", password="password123")
> 
>         # Get first document
>         response = self.client.get(reverse("labeling"))
>         assert response.status_code == 200
>         doc1 = response.context["doc"]
>         assert doc1 is not None
> 
>         # Validate the document
>         post_data = {
>             "doc_id": doc1.id,
>             "surfaces_agricoles": "on",
>             "surfaces_artificialisees": "",
>             "surfaces_forestieres": "on",
>             "surfaces_naturelles": "",
>         }
> 
>         response = self.client.post(reverse("labeling"), post_data)
>         assert response.status_code == 302  # Redirect to next document
> 
>         # Verify document was validated
>         doc1.refresh_from_db()
>         assert doc1.is_validated is True
>         assert doc1.validated_by == self.user1
> 
>         # Should now show the next document
>         response = self.client.get(reverse("labeling"))
>         assert response.status_code == 200
>         doc2 = response.context["doc"]
>         assert doc2 is not None
>         assert doc2 != doc1
> 
>     def test_multiple_users_labeling(self):
>         """Test that multiple users can label documents simultaneously."""
>         # User 1 gets a document
>         self.client.login(username="validator1", password="password123")
>         response = self.client.get(reverse("labeling"))
>         doc1 = response.context["doc"]
> 
>         # Create another client for user 2
>         client2 = Client()
>         client2.login(username="validator2", password="password123")
> 
>         # User 2 should get a different document (not the one locked by user 1)
>         response2 = client2.get(reverse("labeling"))
>         doc2 = response2.context["doc"]
> 
>         assert doc1 != doc2
>         assert doc1.locked_by == self.user1
>         assert doc2.locked_by == self.user2
> 
>     def test_user_cannot_see_locked_document(self):
>         """Test that a user cannot access a document locked by another user."""
>         # User 1 locks a document
>         self.client.login(username="validator1", password="password123")
>         response = self.client.get(reverse("labeling"))
>         doc1 = response.context["doc"]
> 
>         # User 2 tries to validate that same document directly
>         client2 = Client()
>         client2.login(username="validator2", password="password123")
> 
>         post_data = {
>             "doc_id": doc1.id,
>             "surfaces_agricoles": "on",
>             "surfaces_artificialisees": "",
>             "surfaces_forestieres": "",
>             "surfaces_naturelles": "",
>         }
> 
>         response2 = client2.post(reverse("labeling"), post_data)
> 
>         # Should show error, not validate the document
>         assert response2.status_code == 200
>         assert "error" in response2.context
>         
>         doc1.refresh_from_db()
>         assert doc1.is_validated is False
> 
> 
> @pytest.mark.django_db
> class LockExpirationTest(TestCase):
>     """Integration tests for lock expiration behavior."""
> 
>     def setUp(self):
>         """Set up test fixtures."""
>         self.user = User.objects.create_user(
>             username="testuser", password="password123"
>         )
>         self.client = Client()
> 
>     def test_expired_lock_is_released_on_next_request(self):
>         """Test that expired locks are automatically released."""
>         from django.utils import timezone
>         from datetime import timedelta
> 
>         # Create a document with an expired lock (older than 60 minutes)
>         old_time = timezone.now() - timedelta(minutes=90)
>         Document.objects.create(
>             pdf_name="expired_lock_doc.pdf",
>             pdf_text="Document with expired lock",
>             locked_by=self.user,
>             locked_at=old_time,
>         )
> 
>         # Login and request next document
>         self.client.login(username="testuser", password="password123")
>         response = self.client.get(reverse("labeling"))
> 
>         # Should get the document (lock should have been released)
>         assert response.status_code == 200
>         doc = response.context["doc"]
>         assert doc is not None
>         
>         doc.refresh_from_db()
>         assert doc.locked_by is None or doc.locked_by == self.user
> 
> 
> @pytest.mark.django_db
> class EdgeCasesTest(TestCase):
>     """Integration tests for edge cases and error handling."""
> 
>     def setUp(self):
>         """Set up test fixtures."""
>         self.user = User.objects.create_user(
>             username="testuser", password="password123"
>         )
>         self.client = Client()
> 
>     def test_all_documents_validated(self):
>         """Test behavior when all documents are already validated."""
>         # Create and validate a document
>         doc = Document.objects.create(
>             pdf_name="validated_doc.pdf",
>             pdf_text="Already validated content",
>             is_validated=True,
>             validated_by=self.user,
>         )
> 
>         self.client.login(username="testuser", password="password123")
>         response = self.client.get(reverse("labeling"))
> 
>         # Should show empty state
>         assert response.status_code == 200
>         assert response.context["doc"] is None
> 
>     def test_no_documents_in_database(self):
>         """Test behavior when database has no documents."""
>         self.client.login(username="testuser", password="password123")
>         response = self.client.get(reverse("labeling"))
> 
>         # Should show empty state gracefully
>         assert response.status_code == 200
>         assert response.context["doc"] is None
> 
>     def test_concurrent_lock_attempt(self):
>         """Test handling of concurrent lock attempts."""
>         from django.utils import timezone
> 
>         # Create a document
>         doc = Document.objects.create(
>             pdf_name="concurrent_doc.pdf",
>             pdf_text="Document for concurrent access test",
>         )
> 
>         self.client.login(username="testuser", password="password123")
> 
>         # First request locks the document
>         response1 = self.client.get(reverse("labeling"))
>         assert response1.status_code == 200
> 
>         # Manually lock it again (simulating race condition)
>         doc.locked_by = self.user
>         doc.locked_at = timezone.now()
>         doc.save()
> 
>         # Second request should handle gracefully (redirect or show different doc)
>         response2 = self.client.get(reverse("labeling"))
>         assert response2.status_code in [200, 302]
> 
> 
> @pytest.mark.django_db
> class AdminWorkflowTest(TestCase):
>     """Integration tests for admin/re-labeling workflow."""
> 
>     def setUp(self):
>         """Set up test fixtures."""
>         self.admin_user = User.objects.create_superuser(
>             username="admin", password="password123"
>         )
>         self.client = Client()
> 
>         # Create a validated document
>         self.document = Document.objects.create(
>             pdf_name="validated_doc.pdf",
>             pdf_text="Validated content",
>             is_validated=True,
>             validated_by=self.admin_user,
>             validated_predictions={
>                 "surfaces_agricoles": True,
>                 "surfaces_artificialisees": False,
>                 "surfaces_forestieres": True,
>                 "surfaces_naturelles": False,
>             },
>         )
> 
>     def test_admin_can_relabel_document(self):
>         """Test that admin can access and re-label validated documents."""
>         self.client.login(username="admin", password="password123")
> 
>         # Access specific document view (bypasses lock check)
>         response = self.client.get(
>             reverse("labeling_document", kwargs={"doc_id": self.document.id})
>         )
> 
>         assert response.status_code == 200
>         assert response.context["doc"] == self.document
> 
>     def test_revalidation_updates_predictions(self):
>         """Test that re-validation updates predictions correctly."""
>         self.client.login(username="admin", password="password123")
> 
>         # Re-validate with different predictions
>         post_data = {
>             "surfaces_agricoles": "",  # Changed to False
>             "surfaces_artificialisees": "on",  # Changed to True
>             "surfaces_forestieres": "on",
>             "surfaces_naturelles": "",
>         }
> 
>         response = self.client.post(
>             reverse("labeling_document", kwargs={"doc_id": self.document.id}), post_data
>         )
> 
>         assert response.status_code == 302
>         
>         self.document.refresh_from_db()
>         assert self.document.validated_predictions == {
>             "surfaces_agricoles": False,
>             "surfaces_artificialisees": True,
>             "surfaces_forestieres": True,
>             "surfaces_naturelles": False,
>         }
> ```
> 
> ---
> 
> ### 4. Test Configuration File
> 
> You should also create a test settings file:
> 
> ```python
> ae-labeling/ae_labeling/settings_test.py
> ```
> 
> ```python
> """
> Test-specific Django settings.
> """
> from .settings import *
> 
> # Override database for tests
> DATABASES = {
>     "default": {
>         "ENGINE": "django.db.backends.sqlite3",
>         "NAME": ":memory:",
>     }
> }
> 
> # Disable email and other external services in tests
> EMAIL_BACKEND = "django.core.mail.backends.locmem.EmailBackend"
> 
> # Faster password hashing for tests
> PASSWORD_HASHERS = [
>     "django.contrib.auth.hashers.MD5PasswordHasher",
> ]
> 
> # Disable debug toolbar in tests
> if "debug_toolbar" in INSTALLED_APPS:
>     INSTALLED_APPS.remove("debug_toolbar")
> 
> # Add pytest-django to installed apps if needed
> INSTALLED_APPS.append("app.tests")
> ```
> 
> ---
> 
> ### 5. Tests Package Initialization
> 
> Create the test package structure:
> 
> ```python
> ae-labeling/app/tests/__init__.py
> ```
> 
> ```python
> """
> Test suite for the app application.
> """
> ```
> 
> ---
> 
> ## Running the Tests
> 
> To run your tests, you can use either Django's built-in test runner or pytest:
> 
> ### Using Django Test Runner:
> 
> ```bash
> cd ae-labeling
> python manage.py test app.tests --verbosity=2
> ```
> 
> ### Using Pytest (recommended):
> 
> First, install pytest-django if not already installed:
> 
> ```bash
> uv add --dev pytest-django
> ```
> 
> Then create a `pytest.ini` or `pyproject.toml` configuration:
> 
> ```toml
> # Add to ae-labeling/pyproject.toml under [tool.pytest.ini_options]
> [tool.pytest.ini_options]
> DJANGO_SETTINGS_MODULE = "ae_labeling.settings_test"
> python_files = ["test_*.py", "*_test.py"]
> testpaths = ["app/tests"]
> markers = [
>     "django_db: mark test as requiring database access",
>     "locking: tests related to document locking functionality",
>     "querying: tests for model query methods",
> ]
> ```
> 
> Then run:
> 
> ```bash
> cd ae-labeling
> pytest app/tests/ -v
> ```
> 
> ### Running Specific Tests:
> 
> ```bash
> # Run only model tests
> pytest app/tests/test_models.py -v
> 
> # Run only view tests
> pytest app/tests/test_views.py -v
> 
> # Run only integration tests
> pytest app/tests/test_integration.py -v
> 
> # Run tests with specific marker
> pytest -m locking -v
> 
> # Run tests and show coverage
> pytest --cov=app --cov-report=term-missing
> ```
> 
> ---
> 
> ## Summary of Test Coverage
> 
> The test suite I've created covers:
> 
> ### Model Tests (test_models.py):
> - ✅ Document creation and basic attributes
> - ✅ Lock/unlock functionality
> - ✅ Lock expiration logic
> - ✅ Validation workflow
> - ✅ Query methods (`get_next_unlocked_document`)
> 
> ### View Tests (test_views.py):
> - ✅ Authentication requirements for all views
> - ✅ Labeling view GET/POST operations
> - ✅ Specific document access
> - ✅ Lock management API endpoints
> - ✅ Error handling and edge cases
> 
> ### Integration Tests (test_integration.py):
> - ✅ Complete labeling workflow end-to-end
> - ✅ Multi-user scenarios
> - ✅ Concurrent access handling
> - ✅ Lock expiration in real usage
> - ✅ Admin/re-labeling workflow
> - ✅ Edge cases (no documents, all validated, etc.)
> 
> ---
> 
> Would you like me to:
> 1. **Add more specific tests** for any particular functionality?
> 2. **Create fixtures and factories** for easier test setup?
> 3. **Switch to Agent mode** so I can create these files in your project?
> 4. **Add performance/benchmark tests**?
> 5. **Include API documentation** for the test suite?
> 
> Let me know how you'd like to proceed!